<?php

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| Here is where you can register API routes for your application. These
| routes are loaded by the RouteServiceProvider and all of them will
| be assigned to the "api" middleware group. Make something great!
|
*/


/*
Route::resource('categories', App\Http\Controllers\API\CategoryAPIController::class)
    ->except(['create', 'edit']);

Route::resource('companies', App\Http\Controllers\API\CompanyAPIController::class)
    ->except(['create', 'edit']);

Route::resource('branches', App\Http\Controllers\API\BranchAPIController::class)
    ->except(['create', 'edit']);

Route::resource('users', App\Http\Controllers\API\UserAPIController::class)
    ->except(['create', 'edit']);

Route::resource('admins', App\Http\Controllers\API\AdminAPIController::class)
    ->except(['create', 'edit']);

Route::resource('languages', App\Http\Controllers\API\LanguageAPIController::class)
    ->except(['create', 'edit']);

Route::resource('cashback-offers', App\Http\Controllers\API\CashbackOfferAPIController::class)
    ->except(['create', 'edit']);

Route::resource('cashback-coupons', App\Http\Controllers\API\CashbackCouponAPIController::class)
    ->except(['create', 'edit']); */
